[package]
name = "clawpi-agentd"
version = "0.1.0"
edition = "2021"

[dependencies]
clawpi-core = { path = "../clawpi-core" }
serde = "1"
serde_json = "1"
ureq = { version = "=2.9.7", features = ["json"] }
